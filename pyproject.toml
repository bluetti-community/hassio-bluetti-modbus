[tool.mypy]
strict = true
packages = ["custom_components.bluetti_modbus"]
exclude = ["custom_components/bluetti_modbus/vendor/"]

[tool.ruff]
# Vendored from bluetti-modbus-lib, which lints/tests itself in its own repo.
extend-exclude = ["custom_components/bluetti_modbus/vendor"]
