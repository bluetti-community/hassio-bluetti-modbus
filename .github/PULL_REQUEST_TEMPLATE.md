## Description

<!-- What does this PR change, and why? -->

## Related issue

<!-- e.g. "Fixes #123" or "Related to #123". Leave blank if none. -->

## Testing done

<!-- How did you verify this change? -->

## Checklist

- [ ] `coverage run -m unittest discover -s tests -p "*.py"` passes locally (100% coverage on
      `custom_components/*`, excluding `vendor/`, is enforced)
- [ ] `ruff check .` and `mypy` pass locally
- [ ] This PR is focused on a single topic
